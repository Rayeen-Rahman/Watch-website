# ── Frontend Production Environment ──────────────────────────────────────────
# Backend and frontend share the same domain on Hostinger (artifactbd.com).
# Setting VITE_API_URL to empty string means all API calls use relative URLs
# (e.g. /api/products) which route to the same Express server — no CORS needed.

VITE_API_URL=
