# ── Frontend Production Environment ──────────────────────────────────────────
# Backend and frontend share the same domain on Hostinger (artifactbd.com).
# The frontend makes API calls to /api/* on the same origin.

VITE_API_URL=https://artifactbd.com
